@model News.Models.Noticias

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>

    <div>
        <h4>Noticias</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Titulo)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Titulo)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Resumo)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Resumo)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.ListaNI)
            </dt>
            <dd class="col-sm-10">
                @foreach (var img in Model.ListaNI) { 
                    <img src="~/Imagens/@img" alt="" title="" height="300" />
                }
            </dd>
        </dl>
       
    </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
</body>
</html>
